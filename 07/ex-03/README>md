# Character Count via Pipe

## 📌 Description
This project demonstrates **Inter-Process Communication (IPC)** using a **pipe** in C.  
The **parent process** sends a string through a pipe to the **child process**,  
which reads the string, counts the number of characters, and prints the result.