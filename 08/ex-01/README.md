# Simple Message Queue using POSIX

## 📌 Description
This project demonstrates **Inter-Process Communication (IPC)** using **POSIX message queues**.  
The **parent process** sends a message to the **child process**, which reads and prints it.